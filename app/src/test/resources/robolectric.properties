# Render with the native graphics pipeline so Roborazzi captures real pixels,
# and pin to a Robolectric-supported SDK level (the module compiles against 36).
# The locale is pinned so locale-sensitive assertions (formatted dates) stay
# deterministic on any machine or CI runner regardless of the host locale.
sdk=34
graphicsMode=NATIVE
qualifiers=en-rUS
