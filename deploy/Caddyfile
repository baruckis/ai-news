# Caddy obtains and renews the Let's Encrypt certificate for this host
# automatically. `bff` resolves through the Docker Compose network.
# Replace the host below with your own (sub)domain before launching.
news-api.example.com {
	reverse_proxy bff:8080
}
